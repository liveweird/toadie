# Build outputs — rebuilt inside the image
**/build/
.gradle/
web/node_modules/
web/dist/

# IDE / local cruft (.git stays IN the context — the SPA build stage reads the
# commit sha/timestamp from it for the version stamp)
**/.idea/
**/*.iml
server/h2.mv.db
