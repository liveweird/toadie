/// <reference types="vite/client" />

// Injected via `define` in vite.config.ts (git commit stamp for VersionStamp).
declare const __APP_COMMIT__: string;
declare const __APP_COMMIT_TIME__: string;
