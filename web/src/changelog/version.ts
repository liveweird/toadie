// The app's ONE user-facing version, in its own tiny module so the shell (VersionStamp)
// stays lean. When a changelog page arrives (the Lettuce entries.ts pattern), releasing =
// adding the newest entry there AND bumping this literal to its version.
export const APP_VERSION = "0.6.0";
