{
  "identifier": "domain",
  "title": "Domain",
  "description": "A business area that groups related services under one accountable team — think \"Payments\" or \"Storefront\" rather than any one microservice. Start here when you're new: a domain's overview and criticality tell you how carefully to tread.",
  "icon": "Folder",
  "hierarchyRelation": "owned_by",
  "schema": {
    "properties": {
      "overview": {
        "type": "string",
        "title": "Overview",
        "description": "What this domain covers and why it exists, in prose.",
        "format": "markdown"
      },
      "budget_code": {
        "type": "string",
        "title": "Budget code",
        "description": "The finance system's cost-center code for this domain.",
        "pattern": "^[A-Z]{2}-[0-9]{4}$",
        "minLength": 7,
        "maxLength": 7
      },
      "criticality": {
        "type": "string",
        "title": "Criticality",
        "enum": ["low", "medium", "high", "critical"],
        "enumColors": { "low": "pink", "medium": "yellow", "high": "orange", "critical": "red" }
      }
    },
    "required": ["criticality"]
  },
  "relations": {
    "owned_by": {
      "title": "Owned by",
      "description": "The team accountable for this domain.",
      "target": "team",
      "required": false,
      "many": false
    }
  },
  "ownership": {
    "type": "Direct",
    "title": "Owning team"
  }
}
