{
  "identifier": "domain",
  "title": "Domain",
  "description": "A business area that groups systems — Payments, Storefront, Platform. The top of the architecture tree. Backstage kind: Domain (parent_domain → spec.subdomainOf, the team field → spec.owner).",
  "icon": "Folder",
  "hierarchyRelation": "parent_domain",
  "schema": {
    "properties": {
      "type": {
        "type": "string",
        "title": "Type",
        "description": "The Domain type dictionary: what kind of business value the area carries.",
        "enum": ["core-value", "auxiliary", "generic", "tech-foundations"],
        "enumColors": { "core-value": "gold", "auxiliary": "silver", "generic": "lightGray", "tech-foundations": "bronze" }
      },
      "docs": {
        "type": "string",
        "title": "Documentation",
        "description": "Where the domain's documentation lives (Backstage backstage.io/techdocs-ref).",
        "format": "url"
      },
      "links": {
        "type": "array",
        "title": "Links",
        "description": "Further links ({title, url}) — Backstage metadata.links.",
        "items": { "type": "object" }
      }
    }
  },
  "relations": {
    "parent_domain": {
      "title": "Parent domain",
      "description": "The domain this one is a sub-domain of; empty for a top-level domain.",
      "target": "domain",
      "required": false,
      "many": false
    }
  },
  "aggregationProperties": {
    "critical_systems": {
      "title": "Critical systems",
      "target": "system",
      "calculationSpec": { "calculationBy": "entities", "func": "count" },
      "query": {
        "combinator": "and",
        "rules": [
          { "property": "criticality", "operator": "=", "value": "critical" }
        ]
      }
    }
  },
  "ownership": {
    "type": "Direct",
    "title": "Owned by"
  }
}
