{
  "identifier": "_user",
  "title": "User",
  "description": "A person. Kept minimal on purpose: teams own things, people belong to teams. Backstage kind: User (team → spec.memberOf, email → spec.profile.email).",
  "icon": "User",
  "schema": {
    "properties": {
      "email": {
        "type": "string",
        "title": "Email",
        "format": "email"
      },
      "picture": {
        "type": "string",
        "title": "Picture",
        "description": "Avatar URL (Backstage spec.profile.picture).",
        "format": "url"
      },
      "links": {
        "type": "array",
        "title": "Links",
        "description": "Further links ({title, url}) — Backstage metadata.links.",
        "items": { "type": "object" }
      }
    },
    "required": ["email"]
  },
  "relations": {
    "team": {
      "title": "Teams",
      "target": "_team",
      "required": false,
      "many": true
    }
  }
}
